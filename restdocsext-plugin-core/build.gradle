
dependencies {
    compile project(':restdocsext-model')
    
    compile 'org.jsoup:jsoup'
    compile 'commons-io:commons-io'
    optional 'junit:junit'
}

task wrapper(type: Wrapper) {
    gradleVersion = '3.1'
}
